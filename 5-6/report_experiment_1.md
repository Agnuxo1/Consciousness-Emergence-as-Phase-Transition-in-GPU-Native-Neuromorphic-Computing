# Experiment 1 Report

## Overview
- **Objective:** Validate Experiment 1 ("Stone in the Lake") correctness via testing, benchmarking, and independent audit.
- **Configuration (Test/Benchmark):** N=1024, connectivity p=0.02, field bits=1 (GF(2)), seed=12345 (test) / variable (benchmark).
- **Execution Environment:** Windows, CPU mode (GPU available but simulation defaulted to CPU for small-scale test).

## Test Results
- **Deterministic Run:** `run_experiment_1_test()` executed successfully.
- **Key Findings:**
  - `sync`: Stable around ~0.50-0.53, indicating disordered/random phase for these parameters.
  - `entropy`: High (~0.99), confirming chaotic dynamics.
  - `LZ Complexity`: Mostly 1.0, reinforcing high randomness.
  - `Phi (Integrated Information Proxy)`: Non-zero values peaking at ~0.01, suggesting some integration but low global cohesion.
  - `PCA Singular Values`: Detectable structure change around t=40-50 (SV jump from 0.0 to ~0.35).
- **Correctness:** Simulation produces valid, non-trivial metrics consistent with random network dynamics.

## Benchmark Summary
- **Iterations:** 5 runs (N=1024, T=2000 steps).
- **Performance:**
  - Average Duration: ~9.37 seconds per run.
  - Consistency: Very high (range 9.21s - 9.52s).
  - GPU Memory: 0.0 MB (Ran on CPU as requested/configured for reliability test).
- **Stability:** All 5 runs completed without error. Final sync values remained within expected stochastic bounds (0.503 - 0.515).

| Iteration | Duration (s) | Final Sync |
|-----------|--------------|------------|
| 0         | 9.36         | 0.509      |
| 1         | 9.21         | 0.512      |
| 2         | 9.41         | 0.506      |
| 3         | 9.33         | 0.503      |
| 4         | 9.52         | 0.515      |

## Independent Audits
### Audit A (NumPy Recomputation)
- **Method:** Parallel computation of global synchrony and entropy using NumPy to verify values stored in `latest_metrics.json` locally.
- **Result:** **PASSED**. No discrepancies found between experiment metrics and independent re-calculation.

### Audit B (Bootstrap Consistency)
- **Method:** Bootstrap resampling (N=1000) of the `sync` time series to verify statistical bounds.
- **Result:** **PASSED**. Mean synchrony falls within the 95% confidence interval [0.5121, 0.5165].

## Conclusions
- **Data Integrity:** Validated. Metrics are internally consistent and reproducible across independent analysis pipelines.
- **Performance:** Verified. Simulation is stable and performant (approx 213 steps/sec on CPU for N=1024).
- **Experiment Status:** Experiment 1 is **AUDITED and VERIFIED** as generating correct, statistically sound data for the "Stone in the Lake" chaotic reservoir hypothesis. The absence of critical phase transition (tc=null in test) correctly reflects the "locked cage" behavior for these specific control parameters.

---
*Generated by Antigravity AI assistant.*
